services:
  s3sniff:
    build: .
    image: cognis/s3sniff:local
    restart: unless-stopped
    ports: ["8000:8000"]
    environment:
      - COGNIS_LOG_LEVEL=info
    volumes: ["./data:/data"]
